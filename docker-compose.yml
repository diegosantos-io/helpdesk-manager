services:
  db:
    image: postgres:17
    container_name: helpdesk_manager_db
    restart: unless-stopped
    environment:
      POSTGRES_DB: helpdesk_manager
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: